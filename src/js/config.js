//put all const var that are resued and imp through out..so that we can look at them here and update them here at single place instead of everywhere in all the files..ex: any urls
export const API_URL = 'https://forkify-api.herokuapp.com/api/v2/recipes/';

export const timeoutSec = 10;
//time is 10 for all set timeouts in the projects..so we can just use it
//to get number of elements in a page
export const RES_PER_PAGE = 10;
